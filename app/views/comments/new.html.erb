<h1>New Comment</h1>

<%= @comment.errors.full_messages.join(", ") %>

<%= form_for @comment do |f| %>

  <div>
    <%= f.label :body %>
    <%= f.text_area :body %>
  </div>

  <div>
    <%= f.label :post_id %>
    <%= f.text_field :post_id %>
  </div>


  <%= f.submit %>

<% end %>
